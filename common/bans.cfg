// ===================
// server ban settings
// ===================

g_ban_default_bantime "172800"      // 2 days
g_ban_default_masksize "3"          // 3 whole 255.255.255.0 networks (set to 4 for single IPs)

// sync bans using this ban list provider
//g_ban_sync_uri "http://94.23.21.40/~xonotic/bans/"

// accept bans that were initially set on the server IPs listed here
//g_ban_sync_trusted_servers ""

//g_banned_list_idmode 1 // if set to 1 (default), IP banning system always uses the ID over the IP address (so a user in a banned IP range can connect if they have a valid signed ID)
